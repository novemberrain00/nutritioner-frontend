import React, {FC} from 'react';
import Router from '../router/router';

import './app.scss';

const App: FC = (props) => {
  return (
    <>
      <Router/>
    </>
  )
};

export {App};
export default App;
